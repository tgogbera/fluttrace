name: fluttrace
description: "Production-grade frame timing analytics for Flutter. p50/p95/p99 metrics, jank detection, and a pluggable transport pipeline."
repository: https://github.com/tgogbera/fluttrace
version: 1.1.2

topics:
  - flutter
  - performance
  - analytics
  - monitoring

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0

flutter:
  uses-material-design: true